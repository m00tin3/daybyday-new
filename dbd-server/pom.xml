<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.3</version>
    <relativePath/>
  </parent>

  <groupId>com.dbd</groupId>
  <artifactId>dbd-server</artifactId>
  <version>0.1.0</version>
  <name>dbd-server</name>
  <description>Day-BY-Day 论坛后端</description>

  <properties>
    <!-- JDK 版本：编译目标 17（LTS）；本机 JDK 18 可正常编译运行，Spring Boot 3.5 支持 Java 17-24 -->
    <java.version>17</java.version>
    <!-- MyBatis-Plus：Spring Boot 3 专用 starter -->
    <mybatis-plus.version>3.5.9</mybatis-plus.version>
    <!-- SpringDoc：Swagger UI 接口文档 -->
    <!-- 注意：2.6.0 与 Spring Boot 3.5 不兼容 —— 它调用的 ControllerAdviceBean(Object)
         构造函数在 Spring Framework 6.2（Spring Boot 3.5 自带）中已被移除，会导致
         /v3/api-docs 抛 NoSuchMethodError、Swagger 文档整体不可用。
         2.9.1 是官方标注支持 Spring Boot 3.x 的最终版本（3.x 系列仅支持 Spring Boot 4）。 -->
    <springdoc.version>2.9.1</springdoc.version>
  </properties>

  <dependencies>
    <!-- Web：Spring MVC + 内嵌 Tomcat -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <!-- 参数校验：@Valid + jakarta.validation -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <!-- Redis：会话 / 缓存 / 计数 / 排行榜 / 秒杀（项目核心） -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-redis</artifactId>
    </dependency>
    <!-- MyBatis-Plus（Spring Boot 3 专用 starter） -->
    <dependency>
      <groupId>com.baomidou</groupId>
      <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
      <version>${mybatis-plus.version}</version>
    </dependency>
    <!-- MP 3.5.9 起分页等依赖 jsqlparser 的拦截器拆分为独立模块 -->
    <dependency>
      <groupId>com.baomidou</groupId>
      <artifactId>mybatis-plus-jsqlparser</artifactId>
      <version>${mybatis-plus.version}</version>
    </dependency>
    <!-- MySQL 驱动（版本由 Spring Boot parent 管理） -->
    <dependency>
      <groupId>com.mysql</groupId>
      <artifactId>mysql-connector-j</artifactId>
      <scope>runtime</scope>
    </dependency>
    <!-- SpringDoc：/swagger-ui.html 自动生成接口文档（文档驱动联调） -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>${springdoc.version}</version>
    </dependency>
    <!-- Lombok：@Data / @Slf4j 等注解简化代码（编译期生效，不进入产物） -->
    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <optional>true</optional>
    </dependency>
    <!-- 测试 -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <!-- 最终产物名：dbd-server.jar -->
    <finalName>dbd-server</finalName>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <excludes>
            <exclude>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </exclude>
          </excludes>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
